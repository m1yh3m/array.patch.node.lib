const {
  insertAfterPosition,
  insertAtPosition,
  insertBeforePosition,
} = require('./lib')

const {
  patch,
  depatch,
} = require('./patch')

module.exports = {
  insertAfterPosition,
  insertAtPosition,
  insertBeforePosition,
  patch,
  depatch,
}
